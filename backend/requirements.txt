# Install with:  pip install -r requirements.txt
 
fastapi==0.138.2
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
pymysql==1.2.0
python-dotenv==1.2.2
 
# Required by Pydantic's EmailStr (used in schemas.py -> EmployeeCreate).
# Without this, the app crashes on startup with an ImportError.
email-validator==2.3.0
 